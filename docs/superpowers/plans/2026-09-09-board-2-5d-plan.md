# Board 2.5D Implementation Plan

> **For agentic workers:** Implement task-by-task.

**Goal:** Richer 2.5D projector board + realistic SVG snakes; teacher create theme preview aligned.

**Architecture:** CSS-only depth for projector; rewrite `renderSnakePath`/`renderLadderPath`; upgrade create.php theme preview markup/CSS. No backend.

**Tech Stack:** SVG, CSS transforms, existing theme palette vars.

---

### Task 1: Realistic snake (+ ladder polish) SVG

**Files:** `public/assets/app.js`

- [ ] Rewrite `renderSnakePath` with organic curve, thick body, head/eyes, tapering tail
- [ ] Polish `renderLadderPath` rails/rungs with depth classes

### Task 2: Projector 2.5D CSS

**Files:** `public/assets/app.css`

- [ ] Perspective stage, volumetric tiles, atmosphere, pawn standing shadow — scoped under `.projector`

### Task 3: Teacher create preview

**Files:** `app/Views/teacher/games/create.php`, `public/assets/app.css`

- [ ] Richer theme-preview with mini snake/ladder shapes matching family look

### Task 4: Commit + push
